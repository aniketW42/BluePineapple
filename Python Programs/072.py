#Write a python function to check whether the given number can be represented as difference of two squares or not.
import math
def if_diff_of_two_squares(num):
    lb = int(math.sqrt(num))
    ub = num + int(math.sqrt(num))
    
    







    



